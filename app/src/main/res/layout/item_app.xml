<?xml version="1.0" encoding="utf-8"?>
<!--
  Card de app do carrossel. Largura fixa: e o que impede os icones de
  achatarem quando o banner cresce ou quando ha muitos apps. O fundo e
  transparente (app_card_bg) — sem foco aparece so o icone enviado.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="116dp"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:gravity="center"
    android:paddingTop="10dp"
    android:paddingBottom="10dp"
    android:paddingStart="4dp"
    android:paddingEnd="4dp"
    android:focusable="true"
    android:focusableInTouchMode="true"
    android:background="@drawable/app_card_bg"
    android:layout_marginStart="3dp"
    android:layout_marginEnd="3dp">

    <ImageView
        android:id="@+id/app_icone"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:scaleType="fitCenter" />

    <TextView
        android:id="@+id/app_icone_letra"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:gravity="center"
        android:textColor="#FFFFFF"
        android:textSize="26sp"
        android:textStyle="bold"
        android:visibility="gone" />

    <TextView
        android:id="@+id/app_nome"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textColor="#E6FFFFFF"
        android:textSize="13sp"
        android:singleLine="true"
        android:ellipsize="end"
        android:layout_marginTop="10dp" />
</LinearLayout>
